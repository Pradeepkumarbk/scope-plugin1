version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9
    # working_directory: /go/src/github.com/openebs/scope-plugin1
    steps:
      - setup_remote_docker:
          docker_layer_caching: true
      - checkout
      - run: go get -u github.com/openebs/scope-plugin1/metrics
      - run: go build 