defaults: &defaults
  working_directory: /go/src/github.com/openebs/scope-plugin5
  docker:
    - image: circleci/golang:1.9
version: 2
jobs:
  build:
    <<: *defaults
    # docker:
    #   - image: circleci/golang:1.9
    # working_directory: /go/src/github.com/openebs/scope-plugin5
    steps:
      - setup_remote_docker:
          docker_layer_caching: true
      - checkout
      - run: make
      - run: go get -u golang.org/x/lint/golint
      - run: go get golang.org/x/tools/cmd/cover
      - run: go get github.com/mattn/goveralls
      - run: golint
      - run: go test ./...
      - run: go fmt ./...
      - run: go test -v -covermode=count -coverprofile=coverage.out
      - run: $HOME/gopath/bin/goveralls -coverprofile=coverage.out -service=circle-ci -repotoken $COVERALLS_REPO_TOKEN
      - persist_to_workspace:
          root: .
          paths:
          - plugin.tar
  deploy:
    <<: *defaults
    # docker:
    #   - image: circleci/golang:1.9
    # working_directory: /go/src/github.com/openebs/scope-plugin5
    steps:
      - checkout
      - setup_remote_docker
      - attach_workspace:
          at: .
      - run: docker load -i plugin.tar
      - run: |
          test -z "${DOCKER_USER}" && exit 0
          docker login -u $DOCKER_USER -p $DOCKER_PASS
          docker tag openebs/scope-plugin:ci pradeepkumar95/iops:ci
          docker push pradeepkumar95/iops:ci
workflows:
  version: 2
  test_and_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master
    